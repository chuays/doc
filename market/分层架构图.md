```
graph TD
    A[客户端 Android App] -->|HTTPS API| B(API Gateway)
    B --> C[用户服务]
    B --> D[应用管理服务]
    B --> E[下载分发服务]
    B --> F[审核服务]
    B --> G[支付服务 - 可选]

    C --> H[(MySQL: 用户/账户)]
    D --> I[(MySQL: 应用元数据)]
    D --> J[(OSS/S3: APK 存储)]
    E --> K[(Redis: 下载计数/限流)]
    F --> L[(审核队列 + 沙箱系统)]

    M[Vector Agent] -->|日志/指标| N[Vector Aggregator]
    N --> O[(ClickHouse: 日志/行为分析)]
    N --> P[Prometheus/Grafana: 监控]
    
    Q[Cron Job / Flink] -->|聚合统计| O
```
